jest.mock('fs');
describe('csv Data combiner', () => {
  afterAll(() => {
    jest.resetAllMocks();
    jest.clearAllMocks();
  });
  it('should read from file', async () => {
    // TODO:write test case
    expect(true).toBe(true);
  });

  it('should throw error if filename not passed while reading', async () => {
    // TODO:write test case
    expect(true).toBe(true);
  });
  it('should write to file', async () => {
    // TODO:write test case
    expect(true).toBe(true);
  });
  it('should throw error if filename not passed while writing', async () => {
    // TODO:write test case
    expect(true).toBe(true);
  });
  it('should merge the files', async () => {
    // TODO:write test case
    expect(true).toBe(true);
  });
});